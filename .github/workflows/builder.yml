name: Builder
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        run: |
          # 构建 frpc
          sh build.sh frpc frpc-demo1111 0.28.2