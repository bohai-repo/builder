name: Builder
on: [push]
run-name: ${{ github.event.commits[0].message }}
jobs:
  build-frpc-amd64:
    # 构建节点
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        # 匹配关键字触发
        if: ${{ contains(github.event.head_commit.message, 'frpc') }}
        run: |
          # 通知配置
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          # 推送仓库空间
          export build_repo_name='bohai_repo'
          # 推送镜像地址
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          # 仓库登录凭证
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          # 构建参数定义：/bin/bash build.sh 目录名 镜像名 镜像版本
          /bin/bash build.sh frpc frpc-auto 0.28.2
#  build-frpc-armd64:
#    runs-on: [self-hosted,kubernetes-arm64]
#    steps:
#      - uses: actions/checkout@v2
#      - name: Launch Builder
#        if: ${{ contains(github.event.head_commit.message, 'frpc') }}
#        run: |
#          export build_repo_name='bohai_repo'
#          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
#          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
#          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
#          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
#          /bin/bash build.sh frpc frpc-auto-arm 0.28.2
  build-actions-runner-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'github-actions-runner') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr} 
          /bin/bash build.sh github-runner github-runner 2.304.0
  build-actions-runner-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'github-actions-runner') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh github-runner github-runner 2.304.0-arm64
  build-consul-deregister-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'consul') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh consul-deregister consul-deregister 1.0.0-SNAPSHOT
  build-v2ray-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'v2ray') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh v2ray v2ray 4.27.5
  build-v2ray-arm64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'v2ray') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh v2ray v2ray 4.27.5-arm64
#  build-jenkins-slave-arm64:
#    runs-on: [self-hosted,kubernetes-arm64]
#    steps:
#      - uses: actions/checkout@v2
#      - name: Launch Builder
#        if: ${{ contains(github.event.head_commit.message, 'jenkins-slave') }}
#        run: |
#          export build_repo_name='bohai_repo'
#          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
#          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
#          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
#          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
#          /bin/bash build.sh jenkins-slave jenkins-slave-arm64 1.0.0-SNAPSHOT
  build-jenkins-slave-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'jenkins-slave') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh jenkins-slave jenkins-slave-amd64 1.0.0-SNAPSHOT
  build-route-exporter-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'route-exporter') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh route-exporter route-exporter 1.0.0-SNAPSHOT
#  build-php73-arm64:
#    runs-on: [self-hosted,kubernetes-arm64]
#    steps:
#      - uses: actions/checkout@v2
#      - name: Launch Builder
#        if: ${{ contains(github.event.head_commit.message, 'php73') }}
#        run: |
#          export build_repo_name='bohai_repo'
#          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
#          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
#          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
#          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
#          /bin/bash build.sh php73 php-arm 7.3
  build-php73-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'php73') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh php73 php-amd 7.3
  build-openresty-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'openresty') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh openresty openresty 1.25.3
#  build-openresty-arm64:
#    runs-on: [self-hosted,kubernetes-arm64]
#    steps:
#      - uses: actions/checkout@v2
#      - name: Launch Builder
#        if: ${{ contains(github.event.head_commit.message, 'openresty') }}
#        run: |
#          export build_repo_name='bohai_repo'
#          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
#          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
#          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
#          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
#          /bin/bash build.sh openresty openresty-arm 1.21.4-waf
  build-wrest-webhook-python-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'wrest-webhook-python') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh wrest-webhook-python wrest-webhook-python 1.1.2-SNAPSHOT
  build-mail-webhook-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'mail-webhook') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh mail-webhook mail-webhook 1.0.0-SNAPSHOT
  # build-remote-download-service-amd64:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: Launch Builder
  #       if: ${{ contains(github.event.head_commit.message, 'remote-download-service') }}
  #       run: |
  #         export build_repo_name='bohai_repo'
  #         export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
  #         export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
  #         export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
  #         docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
  #         /bin/bash build.sh remote-download-service remote-download-service 1.0
  build-remote-download-service-arm64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'remote-download-service') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh remote-download-service remote-download-service 1.0.0-ARM64
  build-rss-manager-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'rss-manager') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          export NOTICE_WECHAT=true          
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh rss-manager rss-manager 1.0.0-SNAPSHOT
  build-shell-builder-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'shell-builder') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.cn-hangzhou.aliyuncs.com'
          export NOTICE_PATH=${{ secrets.NOTICE_PATH }}
          export NOTICE_MAIL=${{ secrets.NOTICE_MAIL }}
          export NOTICE_WECHAT=false          
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh shell-builder shell-builder 1.0.0-SNAPSHOT
  build-kubedoor-agent-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'kubedoor') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh kubedoor-agent kubedoor-agent 1.3.2-arm64
  build-kubedoor-alarm-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'kubedoor') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh kubedoor-alarm kubedoor-alarm 1.3.2-arm64
  build-kubedoor-master-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'kubedoor') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh kubedoor-master kubedoor-master 1.3.2-arm64
  build-kubedoor-mcp-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'kubedoor') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh kubedoor-mcp kubedoor-mcp 1.3.2-arm64
  build-kubedoor-web-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'kubedoor') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh kubedoor-web kubedoor-web 1.3.2-arm64
  build-joyagent-jdgenie-armd64:
    runs-on: [self-hosted,kubernetes-arm64]
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        if: ${{ contains(github.event.head_commit.message, 'joyagent-jdgenie') }}
        run: |
          export build_repo_name='bohai_repo'
          export build_repo_addr='registry.ap-northeast-1.aliyuncs.com'
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} ${build_repo_addr}
          /bin/bash build.sh joyagent-jdgenie joyagent-jdgenie 1.0.0-arm64