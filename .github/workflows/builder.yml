name: Builder
on: [push]
run-name: ${{ github.actor }} Submitted Build 🚀
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Launch Builder
        run: |
          docker login --username=${{ secrets.ALIYUN_USERNAME }} --password=${{ secrets.ALIYUN_PASSWORD }} registry.cn-hangzhou.aliyuncs.com
          /bin/bash build.sh frpc frpc-auto 0.28.2
          /bin/bash build.sh frpc frpc-auto01 0.28.2
          /bin/bash build.sh frpc frpc-auto02 0.28.2
          /bin/bash build.sh frpc frpc-auto04 0.28.2